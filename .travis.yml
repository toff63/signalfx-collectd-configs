before_install:
  - sudo apt-get update
install:
  - 
  - sudo apt-get install collectd
  - sudo mkdir /etc/collectd.d/managed_config
  - sudo cp managed_config/* /etc/collectd.d/managed_config/
  - sudo cp collectd.conf /etc/collectd.d/

script: sudo collectd -f
